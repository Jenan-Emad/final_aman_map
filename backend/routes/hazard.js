// const express = require("express");
// const hazardController = require("../controllers/HazardController");
// const logger = require("../middlewares/logger");

// const router = express.Router();

// router.post("/createHazard", logger,  hazardController.createHazard)
//       .get('/', hazardController.getHazards)
//       .get('/:id', hazardController.getHazardById)
//       .put('/:id', hazardController.updateHazard)
//       .delete('/:id', hazardController.deleteHazard);

// module.exports = router;